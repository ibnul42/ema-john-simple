import React from 'react';

const ReviewItem = (props) => {
    console.log(props)
    return (
        <div>
            <h3>This is to Review</h3>
        </div>
    );
};

export default ReviewItem;